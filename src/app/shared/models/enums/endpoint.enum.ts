export enum Endpoint {
  RES_REQUEST = 'reservation_request',
  ROOM = 'room',
  MEETING_ROOM = 'meeting_room',
  RESERVATION = 'reservation',
  RESOURCE_CAPACITY_UTILIZATION = 'resource_capacity_utilization',
}
